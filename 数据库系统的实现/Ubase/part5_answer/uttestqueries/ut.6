/*
 * ut.6: 测试容错能力
 */

/* 你的程序是否检测了同名字段? */
/* 下列语句应当失败 */
create table ned (ted char(24), ted int);

/* 你的程序是否检测了表已经存在? */
create table ned (ted char(24), jed int);	/* <-- 这条语句应当成功 */
create table ned (ted char(24), jed int);	/* <-- 这条语句应当失败 */

/* 你的程序是否检测了超大的表? */
/* 下列语句应当失败 */
create table red(ted char(254), jed char(254), red char(254), ed char(254), fred char(254), ned char(254), aed char(254), bed char(254), ced char(254), ded char(254), eed char(254), fed char(254), ged char(254), hed char(254), ied char(254), ked char(254), led char(254)); 

/* 你能删除不存在的表吗? */
/* 下列语句应当失败 */
destroy table jed;

/* 你能多次删除同一个表吗? */
destroy table ned;	/* <-- 这条语句应当成功 */
destroy table ned;	/* <-- 这条语句应当失败 */

/* 重新创建ned表 (应当成功) */
create table ned (ted char(24), jed int);


/* 你能用print显示不存在的表内容吗？*/
print table jed;

/* 你能从不存在的文件导入数据吗? */
load table ned from ("no-such-file");

/* 你能将文件数据导入不存在的表吗? */
load table ed from ("../data/soaps.data");

/* 你能用help命令显示不存在的表结构吗? */
help table jed;

/* 下列测试均应当成功 */

/* 数据库中剩下的东西应当满足什么条件? */
help;
help table ned;
!ls

quit;
