/*
 * ut.5: 测试load和print命令
 */

/* 创建2个表 */
create table soaps(soapid int, sname char(28), network char(4), rating real);
create table stars(starid int, stname char(20), plays char(12), soapid int);

/* 从../data/stars.data导入记录 */
load table soaps from ("../data/soaps.data");

/* 显示输出soaps表的内容 */
print table soaps;

/* 创建索引，本课程不做要求 */
/* buildindex soaps(soapid);*/
/* buildindex stars(stname);*/

/* 从../data/stars.data导入记录 */
load table stars from ("../data/stars.data");

/* 显示输出stars表的内容 */
print table stars;

/* 显示输出两个表的结构 */
help table soaps;
help table stars;

quit;
